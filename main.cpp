#include <iostream>
using namespace std;

int main() {
    // liczba turniejów
    int T;
    cin >> T;
    for (int i = 0; i < T; i++) {
        // budżet, liczba zawodników
        int B,n;
        cin >> B >> n;
        for (int j = 0; j < n * (n - 1) / 2; j++) {
            // x gra przeciwko y, zwycięża w, łapówka b
            int x, y, w, b;
            cin >> x >> y >> w >> b;
        }
    }

}
